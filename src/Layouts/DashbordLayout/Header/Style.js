import { styled } from "@mui/material/styles";

export const HeaderBox = styled("div")({
    ".logo": {
        fontWeight: 'bold',
        fontSize: '24px',
        color: '#FFFFFF', 
        letterSpacing: '2px',
      },
});
